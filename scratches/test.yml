apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: '{name}'
  name: '{name}'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: '{name}'
  strategy: {}
  template:
    metadata:
      labels:
        app: '{name}'
    spec:
      containers:
      - image: test
        name: test
        resources: {}
status: {}
